# @summary Configure TCP wrappers
#
# Configure TCP wrappers
#
# @example
#   include lsys::hardening::tcp_wrappers
class lsys::hardening::tcp_wrappers {
  # no configuration - use lsys::hardening::tcp_wrappers::noop
}
